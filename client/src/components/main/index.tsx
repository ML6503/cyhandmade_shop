import React from 'react';
import Shop from './Shop';
import Slider from './Slider';

const MainPage = () => {

    return (
        <section>
            <Slider />
            <Shop />
        </section>
    );
};

export default MainPage;
